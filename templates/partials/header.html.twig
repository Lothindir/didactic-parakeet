<header class="bg-blue-parakeet p-2 z-10 h-30 fixed w-full md:pl-32 lg:pl-64 md:pr-10 lg:pr-20 flex items-center justify-between flex-wrap border-b-2 border-blue-800">
    <a href="/">
        <div class="flex items-center flex-shrink-0 text-white mr-6">
            <img src="{{ asset('images/logo.png') }}" alt="Notre petit perroquet" class="w-24">
            <span class="font-semibold text-5xl ml-2 tracking-tight">Didactic Parakeet</span>
        </div>
    </a>
    <div class="text-lg lg:mt-0 text-black text-right">

        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <p class="font-semibold text-xl">Bienvenue {{ app.user.username }}</p>
            <a href="/logout" class="hover:text-white">
                Se déconnecter
            </a>
        {% else %}
            <a href="/login" class="mr-4 hover:text-white">
                Se connecter
            </a>
            <a href="/register" class="hover:text-white">
                S'inscrire
            </a>
        {% endif %}
    </div>
</header>
